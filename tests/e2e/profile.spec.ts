import { test, expect } from '@playwright/test';

test.describe('Profile Flow', () => {
  test('should view own profile', async ({ page }) => {
    test.skip(); // Requires authentication
  });

  test('should tap player in leaderboard to view profile', async ({ page }) => {
    test.skip(); // Requires authentication
  });

  test('should view another player profile', async ({ page }) => {
    test.skip(); // Requires authentication
  });
});
